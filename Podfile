# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'TestRxDataSource' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'RxSwift',    '~> 3.0'
  pod 'RxCocoa',    '~> 3.0'
  pod 'RxDataSources’

  target 'TestRxDataSourceTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TestRxDataSourceUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
